/*using LitJson;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SauvegardeTemps : SauvegardeBase
{
    
    public override JsonData SavedData()
    {
        //JsonData data = SavedTransform;
        //data["NumeroJour"] = GetComponent<GameManager>().NumeroJour.ToString();
        //data["Heure"] = GetComponent<GameManager>().heure.ToString();
        //data["$saveID"] = SaveID;

        //Debug.Log("GameManager SavedData: " + data.ToJson());
        //return data;
    }
    public override void LoadFromData(JsonData data)
    {
        //GetComponent<GameManager>().NumeroJour = (int)data["NumeroJour"];
        //GetComponent<GameManager>().heure = (float)data["Heure"];

        //GetComponent<GameManager>()._soleil.modifierHeure(GetComponent<GameManager>().heure);

    }
    
}
*/
